# Программный комплекс для интерактивного моделирования и визуализации процессов

## Описание проекта

Дипломная работа на тему: **"Проектирование и разработка программного комплекса для интерактивного моделирования и визуализации процессов"**

Проект представляет собой универсальную систему для визуализации и пошагового выполнения различных алгоритмов и процессов в 3D пространстве, разработанную на Unity 6.

## Основные возможности

### 🎯 Функциональность

- **Интерактивная 3D визуализация** алгоритмов и процессов
- **Пошаговое выполнение** с полным контролем скорости
- **Статистика в реальном времени**: сравнения, обмены, шаги
- **Гибкая система управления** камерой для просмотра с любого угла
- **Модульная архитектура** для легкого добавления новых алгоритмов

### 📊 Реализованные алгоритмы сортировки

1. **Пузырьковая сортировка (Bubble Sort)**
   - Временная сложность: O(n²)
   - Пространственная сложность: O(1)
   - Простой алгоритм сравнения соседних элементов

2. **Быстрая сортировка (Quick Sort)**
   - Временная сложность: O(n log n) средняя, O(n²) худшая
   - Пространственная сложность: O(log n)
   - Эффективный алгоритм "разделяй и властвуй"

3. **Сортировка вставками (Insertion Sort)**
   - Временная сложность: O(n²)
   - Пространственная сложность: O(1)
   - Построение отсортированного массива по одному элементу

4. **Сортировка выбором (Selection Sort)**
   - Временная сложность: O(n²)
   - Пространственная сложность: O(1)
   - Поиск минимального элемента и его перемещение

## 🏗️ Архитектура системы

### Структура проекта

```
Assets/_Project/
├── Scripts/
│   ├── Core/                  # Ядро системы
│   │   ├── IProcess.cs        # Интерфейс процесса
│   │   ├── SimulationState.cs # Состояния симуляции
│   │   ├── SimulationManager.cs # Менеджер симуляции
│   │   ├── VisualizationController.cs # Контроллер визуализации
│   │   └── ApplicationController.cs # Главный контроллер
│   ├── Processes/             # Базовые классы процессов
│   │   └── SortingAlgorithm.cs # Базовый класс сортировки
│   ├── Algorithms/            # Реализации алгоритмов
│   │   ├── BubbleSort.cs
│   │   ├── QuickSort.cs
│   │   ├── InsertionSort.cs
│   │   └── SelectionSort.cs
│   ├── Visualization/         # Система визуализации
│   │   ├── VisualizationElement.cs # Базовый визуальный элемент
│   │   └── BarVisualization.cs # Столбчатая визуализация
│   ├── UI/                    # Пользовательский интерфейс
│   │   └── UIManager.cs
│   └── Camera/                # Управление камерой
│       └── CameraController.cs
├── Scenes/
│   └── SampleScene.unity
├── Prefabs/
└── Materials/
```

### Основные компоненты

#### 1. SimulationManager
Центральный менеджер для управления симуляцией:
- Запуск/пауза/остановка
- Пошаговое выполнение
- Управление скоростью
- События состояния

#### 2. VisualizationController
Управление визуальными элементами:
- Создание 3D объектов
- Анимации перемещения и обмена
- Цветовая индикация состояний
- Управление layout

#### 3. IProcess
Унифицированный интерфейс для всех процессов:
```csharp
public interface IProcess
{
    string ProcessName { get; }
    string Description { get; }
    ProcessCategory Category { get; }
    string TimeComplexity { get; }
    string SpaceComplexity { get; }

    void Initialize();
    bool Step();
    IEnumerator Execute();
    void Reset();
    float GetProgress();

    event EventHandler<ProcessStepEventArgs> OnStepCompleted;
}
```

## 🎮 Управление

### Горячие клавиши

- **Space** - Запуск/Пауза симуляции
- **N** - Генерация нового случайного массива
- **R** - Сброс камеры к начальному положению
- **F** - Фокус камеры на визуализации

### Управление камерой

- **ПКМ + движение мыши** - Вращение камеры
- **WASD** - Перемещение камеры
- **Q/E** - Вверх/вниз
- **Shift + движение** - Ускоренное перемещение
- **Колесо мыши** - Приближение/отдаление

### UI элементы

- **Play** - Запуск автоматического выполнения
- **Pause** - Пауза выполнения
- **Step** - Выполнить один шаг
- **Reset** - Сброс к начальному состоянию
- **Speed +/-** - Изменение скорости симуляции

## 🔧 Технические требования

### Минимальные требования

- Unity 6.0 или выше
- .NET Standard 2.1
- DOTween (для анимаций)
- TextMeshPro (для UI)

### Зависимости

Проект использует следующие пакеты Unity:
- `com.unity.textmeshpro` - Текстовый UI
- `com.demigiant.dotween` - Система анимаций (опционально, можно заменить на Unity Animation)

## 📝 Расширение функциональности

### Добавление нового алгоритма

1. Создайте класс, наследующий `SortingAlgorithm` или реализующий `IProcess`
2. Реализуйте абстрактные методы:
   ```csharp
   public class MyCustomSort : SortingAlgorithm
   {
       public override string ProcessName => "Моя сортировка";
       public override string Description => "Описание алгоритма";
       public override string TimeComplexity => "O(?)";
       public override string SpaceComplexity => "O(?)";

       public override bool Step()
       {
           // Логика одного шага
       }
   }
   ```
3. Добавьте алгоритм в `ApplicationController.AlgorithmType`

### Создание нового типа визуализации

1. Наследуйтесь от `VisualizationElement`
2. Переопределите `UpdateVisualization()`
3. Добавьте специфичные методы анимации

## 📊 Особенности реализации

### Пошаговое выполнение

Каждый алгоритм разбит на атомарные шаги, что позволяет:
- Полностью контролировать скорость выполнения
- Наблюдать каждую операцию отдельно
- Понимать работу алгоритма на визуальном уровне

### Анимационная система

Используется DOTween для плавных анимаций:
- Перемещение элементов
- Изменение цвета
- Масштабирование
- Появление/исчезновение

### Событийная модель

Система основана на событиях для связи компонентов:
```csharp
SimulationManager.Instance.OnStateChanged += HandleStateChange;
SimulationManager.Instance.OnProcessStep += HandleStep;
SimulationManager.Instance.OnSimulationCompleted += HandleComplete;
```

## 🎓 Применение в образовании

Проект подходит для:
- Изучения алгоритмов сортировки
- Визуализации временной сложности
- Сравнения эффективности алгоритмов
- Демонстрации работы различных подходов
- Интерактивного обучения программированию

## 🚀 Будущее развитие

### Планируемые возможности

- [ ] Алгоритмы поиска в графах (BFS, DFS, Dijkstra, A*)
- [ ] Визуализация деревьев (BST, AVL, Red-Black)
- [ ] Физические симуляции (гравитация, столкновения)
- [ ] Сетевые процессы (маршрутизация, протоколы)
- [ ] Экспорт статистики в CSV/JSON
- [ ] Запись и воспроизведение сессий
- [ ] Множественные режимы визуализации
- [ ] Редактор пользовательских данных

## 👨‍💻 Автор

Дипломная работа, 2025

## 📄 Лицензия

Образовательный проект
